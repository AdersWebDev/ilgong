/**
 * detail.overall.js
 * - 건물 종합평가(구현 예정 블럭)
 * - 현재는 UI만 존재하며 API/저장 로직 없음
 */
(() => {
  // placeholder: 추후 API 연동 예정
  const textarea = document.getElementById('buildingOverallEvaluation');
  if (textarea) {
    textarea.setAttribute('data-impl', 'pending');
  }

  window.BuildingDetailOverall = {};
})();

